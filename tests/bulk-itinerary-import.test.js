'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const {parseBulkEntries,mergeRecords,joinBatchMaterials,withTimeout,validateParsedResult}=require('../src/bulk-itinerary-import.js');
test('accepts multiple tour codes and URLs separated by lines spaces or commas',()=>{const entries=parseBulkEntries('asb07br261226dw\nPUS05BX261002J, https://www.besttour.com.tw/itinerary/FUK05BR261206FU');assert.deepEqual(entries,['ASB07BR261226DW','PUS05BX261002J','https://www.besttour.com.tw/itinerary/FUK05BR261206FU'])});
test('removes duplicate bulk entries',()=>{assert.deepEqual(parseBulkEntries('PUS05BX261002J PUS05BX261002J'),['PUS05BX261002J'])});
test('bulk records update existing tours instead of duplicating them',()=>{const result=mergeRecords([{code:'AAA05BR261001A',title:'舊名稱',price:'30,000元'}],[{code:'AAA05BR261001A',title:'新名稱',price:'29,000元'}]);assert.equal(result.length,1);assert.equal(result[0].title,'新名稱');assert.equal(result[0].price,'29,000元')});
test('batch marketing materials contain one separate copy for every imported trip',()=>{const result=joinBatchMaterials([{record:{code:'SPK06TR261203RG'},outputs:{lineOut:'北海道六日文案',edmOut:'北海道 EDM'}},{record:{code:'SPK05BR261116BY'},outputs:{lineOut:'北海道五日文案',edmOut:'北海道五日 EDM'}}]);assert.match(result.lineOut,/第 1 團｜SPK06TR261203RG/);assert.match(result.lineOut,/北海道六日文案/);assert.match(result.lineOut,/第 2 團｜SPK05BR261116BY/);assert.match(result.lineOut,/北海道五日文案/);assert.match(result.edmOut,/北海道 EDM/);assert.match(result.edmOut,/北海道五日 EDM/)});
test('hard timeout advances even when the underlying request never settles',async()=>{let timedOut=false;await assert.rejects(withTimeout(new Promise(()=>{}),15,()=>{timedOut=true}),/等待超過/);assert.equal(timedOut,true)});
test('rejects an official response that has no usable title or price',()=>{assert.throws(()=>validateParsedResult({confidence:{title:false,price:false,dates:true}}),/行程名稱、售價/)});
